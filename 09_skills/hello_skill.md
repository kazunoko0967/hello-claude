# カスタムスキルのサンプル

このファイルを `.claude/commands/hello.md` にコピーすると、
`/hello` コマンドとして使えるようになる。

## ファイル内容（.claude/commands/hello.md に配置するもの）

```markdown
現在のリポジトリの状態を以下の形式で報告してください：

1. 変更されたファイルの一覧
2. 現在のブランチ名
3. 最新のコミットメッセージ
4. 一言コメント（絵文字付き）
```

## 使い方

```
/hello
```
