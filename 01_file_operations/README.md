# 01 ファイル操作

Claude Codeのファイル読み書き・編集機能を体験する。

## お題

`sample.py` にバグと未実装関数がある。Claude Codeに以下を依頼してみよう。

1. **読む**: 「sample.pyを読んで内容を説明して」
2. **バグ修正**: 「sample.pyのバグを直して」
3. **関数追加**: 「greet関数にname引数を追加して、'Hello, {name}!'を返すようにして」
4. **新規作成**: 「utils.pyを作成して、文字列を逆順にするreverse_string関数を追加して」
